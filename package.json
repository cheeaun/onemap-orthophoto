{
  "name": "onemap-orthophoto",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "maplibre-gl": "^5.23.0"
  },
  "devDependencies": {
    "vite": "^8.0.8"
  }
}
